# Relaybox Example

Run the example consumer:

```bash
go run ./example/basic_consumer
```

Expected behavior:

- the first message is processed
- the duplicate is skipped
- the program exits successfully

